# Copy this file to .env and set the values. Do not commit .env to version control.
# See README.md for details.

# Auth — required for sign-in
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Database — SQLite (default)
DATABASE_URL="file:./dev.db"

# OpenAI — required for expert matching
# https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Opik — LLM tracing (part of stack; set for Encode hackathon judging)
# Get API key: https://www.comet.com/opik (your workspace → Get started)
# Docs: https://www.comet.com/docs/opik/reference/typescript-sdk/overview
OPIK_API_KEY=
OPIK_URL_OVERRIDE=https://www.comet.com/opik/api
OPIK_PROJECT_NAME=knowledgeshare
OPIK_WORKSPACE_NAME=
